# 2025-05-17

- 为 React + TypeScript + SVG 线路几何可视化项目生成 README.md
- 为 AlignmentView.tsx 新增 Hover Tooltip 功能：每段 50 点采样透明圆捕获 hover，显示里程/X/Y/方位角，黄色高亮点 + 深色毛玻璃 tooltip，absolute div 定位
- 为 AlignmentView.tsx 新增 currentDirection prop，在 currentPoint 上绘制切线箭头（长度 40，绿色，SVG marker 箭头），App.tsx 查询后传入 tangent 数据
- 为 AlignmentView.tsx 新增路段标签：中点位置显示 LINE / ARC R=xxx，SVG text + 半透明背景矩形，颜色与线段类型一致
- 为 AlignmentView.tsx 新增 currentMileage prop，SVG 左上角显示 Mileage: xxx m（16px 加粗，半透明白色背景），App.tsx 查询成功后传入
